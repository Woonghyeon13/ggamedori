<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="game.dori.mapper.wishlistMapper">

	<select id="selectWishlist" resultType="wishlistVO">
		SELECT * FROM wishlist_tb, product_tb,
		WHERE wishlist_tb.MEMBER_TB_IDX = member_tb.MEMBER_IDX
			and member_tb_idx = #{member_idx}
	</select>

</mapper>