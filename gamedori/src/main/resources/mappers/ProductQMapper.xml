<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="game.dori.mapper.productQMapper">
	<!-- 마이페이지 상품문의 리스트 출력 -->
	<select id="selectList" resultType="PRODUCT_Q_VO" parameterType="int">
		select *
			from PRODUCT_Q_TB
			where MEMBER_TB_IDX = #{MEMBER_IDX}
	</select>
	
	<!-- 관리자페이지 상품문의 리스트 출력 -->
	<select id="pqlist" resultType="pqlistVO">
		select *
			from
				product_q_tb pq,
				member_tb m,
				product_tb p
			where
				pq.product_tb_idx = p.prod_idx
				and
				pq.member_tb_idx = m.member_idx
	</select>
	
	<!-- 상품문의 답변 -->
	<update id="pqAnswer" parameterType="pqlistVO">
		update product_q_tb
			set
				prod_q_reply = #{prod_q_reply},
				prod_q_yn = 1
			where
				prod_q_idx = #{prod_q_idx}
	</update>
	
</mapper>